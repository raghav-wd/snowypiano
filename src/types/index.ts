export type ThemeState = {
    notation: string,
    line: string,
    clef: string,
}

export type ClefConfig = {
    notationSize: number,
    lineSpace: number,
    lineThinkness: number,
}